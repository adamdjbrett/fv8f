<section class="container">
<div class="row patners justify-content-center mb-5">
<div class="col-md-12 p-3 p-md-5 text-center">
<h3 class="promo_text mb-3">{{metadata.patners.title}}</h3>
</div>
{% for p in metadata.patners.item %}<div class="col-md-2 col-4 p-3">
<a href="{{p.url}}" title="{{p.title}}" target="_blank">
<img class="lazy shadow-sm rounde img-fluid" alt="{{p.title}}" width="100%" height="100%" loading="lazy" 
data-src="{{p.image}}"/>
</a>
</div>{% endfor %}
<div class="p-3 p-md-3"></div>
</div>
</section>
