<header class="container-fluid bb">
<div class="row">
<div class="col-md-11 p-3 p-md-5 mt-5 position-relative">
  <div class="header-content">
    <p class="sub_hero">{{metadata.title}}</a>
    </p>
    <h1 class="hero_text"><a class="text-white" href="{{page.url or metadata.url}}">
    {{title or metadata.title}}</a>
    </h1>
  </div>
{% if image %}<img data-src="{{image}}" alt="{{title}}" class="header-image lazy responsive-image ms-3">{% endif %}
</div>
<div class="p-3"></div>
</div>
</header>
